# 安卓 App 打包安装 · 手把手教程

> 全程在**网页上点鼠标**完成，**不用装任何软件、不用敲命令**。
> 唯一需要的前提：一个 GitHub 账号（免费，没有就花 2 分钟注册一个）。

**先说清楚**：App 和现在的网页**数据完全一样**，共用同一套云端数据。App 只是给安卓套了个壳，
把"读缓存"这个毛病关掉。装了 App，安卓就稳了；网页端在电脑和苹果上照常用。

**装好之后你会用到的（相比现在的网页）**：

| 功能 | 怎么用 |
|---|---|
| 点开就是 Logo，不再白屏 | 自动（启动闪屏） |
| 页面秒开 | 自动（业务页已缓存在手机本地） |
| 下拉刷新 | 页面滚到顶部后，手指往下拉 |
| 打印 A4 单据 | 右上角菜单 ⋮ → **打印当前页面（A4）** |
| 退出 App | 在首页**连按两次**返回键 |
| 网页改版后更新 | 菜单 ⋮ → **检查页面更新**（或退出重开，自动生效） |

**往后改网页里的功能，不用重新打包 App**，下次启动自动就是新版。

---

## 第 0 步：准备 GitHub 账号（已有就跳过）

1. 浏览器打开 https://github.com
2. 点右上角 **Sign up**，用邮箱注册（跟着提示走，会让你验证邮箱）
3. 注册完 **Sign in** 登录

---

## 第 1 步：新建一个仓库

1. 登录后，点右上角头像左边的 **+** 号 → **New repository**
2. 按下图填：

   | 项目 | 填什么 |
   |---|---|
   | Repository name | `wanlongzhou-supply`（随便起，英文就行） |
   | 说明（Description） | 可以不填 |
   | **Public / Private** | 选 **Private（私有）** 或 Public 都行，建议 Private |
   | 下面三个勾选项 | **全部不要勾**（不要勾 Add a README） |

3. 点绿色按钮 **Create repository**

   成功后会跳到一个"空仓库"页面，上面有一行提示 "or upload an existing file" 之类的链接。

---

## 第 2 步：上传工程文件（关键：别多套一层目录）

1. 在电脑上打开这个文件夹：
   `D:\WORKBUDDY\hotel_requisition_project\android-app`

2. **进到 `android-app` 里面去**，然后**全选里面的所有文件和文件夹**
   （`app` 文件夹、`gradle` 文件夹、`build.gradle`、`settings.gradle`、
   `gradle.properties`、`README-构建说明.md` 等等，**全部选中**）

3. 回到刚才那个空仓库页面，点 **uploading an existing file** 这个链接

4. 把刚才选中的**所有内容**拖到网页上的虚线框里，松开鼠标

   > ⚠️ **最容易错的一步**：要拖的是 `android-app` **里面**的东西，
   > 不要拖 `android-app` 这个文件夹本身。
   > 判断标准：传完之后打开仓库首页，**应该直接就能看到 `settings.gradle`**，
   > 不需要点进任何文件夹。如果你看到的是一个 `android-app` 文件夹，那就是套错层了，
   > 删掉重传。

5. 等文件上传完（下面会出现文件列表），点绿色按钮 **Commit changes**

---

## 第 3 步：创建打包配置文件（**别漏，漏了就打不了包**）

> 这一步是给 GitHub 下"怎么打包"的指令。
> 因为第 2 步拖文件夹时，隐藏文件夹 `.github` 有可能会丢，所以我们手动建一个，更保险。

1. 回到仓库首页，点 **Add file** → **Create new file**

2. 最上面**文件名**那个框，一字不差地输入：
   ```
   .github/workflows/build-apk.yml
   ```
   （**最前面有个点**，GitHub 会自动帮你建出 `.github` 和 `workflows` 两层目录）

3. 打开电脑上这个文件，把内容**全部复制**：
   `D:\WORKBUDDY\hotel_requisition_project\android-app\【复制用】构建配置.txt`
   （从 `name: 构建安卓 APK` 那一行开始，一直复制到文件最后）

4. 粘贴到网页的大文本框里

5. 点绿色按钮 **Commit changes**

   传完之后，仓库首页应该能看到一个 `.github` 文件夹。

---

## 第 4 步：开始打包

1. 点仓库页面顶部的 **Actions** 标签

2. 如果页面中间有提示让启用工作流（"I understand my workflows, go ahead and enable them"），
   点那个绿色按钮启用

3. 左边列表里点 **构建安卓 APK**

4. 右边会出现一个 **Run workflow** 按钮，点它旁边的小下拉箭头 →
   分支选 `main` → 再点绿色 **Run workflow**

5. 页面刷新后，列表里会出现一条正在跑的记录，前面是**黄色转圈的圆点**

6. 等 **5～10 分钟**，圆点变成**绿色对勾 ✓** 就成功了
   （第一次会慢一些，之后会快一点）

---

## 第 5 步：下载安装包（APK）

1. 点进那条绿色的运行记录

2. 页面往下拉到最底部，找到 **Artifacts** 区域

3. 点 **wanlongzhou-supply-debug**，浏览器会下载一个 zip 压缩包

4. 解压这个 zip，里面有个 **`app-debug.apk`** —— 这就是手机安装包

---

## 第 6 步：装到手机上

### 把 APK 弄到手机（选一个最方便的）

| 方法 | 怎么做 |
|---|---|
| **微信/QQ（最快）** | 电脑微信发给「文件传输助手」→ 手机上打开 → 下载 → 点开安装 |
| **数据线** | 拷到手机存储里 → 手机用「文件管理」找到它 → 点开安装 |
| **网盘** | 传到网盘 → 手机下载 → 安装 |

### 安装时会遇到的提示

- 安卓会弹 **「未知来源应用」/「允许安装未知应用」** → 点**允许**（按提示操作即可，
  不同品牌手机会跳到设置页，把对应开关打开就行）
- 有的手机会要求输锁屏密码、或者让你确认"风险应用" → 照着点确认

> 装完之后桌面会出现一个**绿色箱子图标**，名字叫「万龙洲供应链」，点开就能用。
> 首次打开会加载几秒，之后就跟网页一模一样，登录、申购、发货全都照旧。

---

## 出问题怎么办

| 现象 | 原因 / 解决 |
|---|---|
| Actions 里找不到「构建安卓 APK」 | 第 3 步没成功。检查文件名是不是 `.github/workflows/build-apk.yml`（前面有个点） |
| 打包失败（红色 ✗） | 点进红色记录，展开红色的那一步，把报错内容**截图发我**，我来看 |
| 刚点运行就秒失败 | 多半是第 2 步套错层了。确认仓库首页直接能看到 `settings.gradle` |
| 手机上装不上 | 看是不是「未知来源」没允许，去 设置 → 安全 → 允许安装未知应用 |
| 打开 App 白屏 | 检查手机联网；或右上角菜单 ⋮ → **刷新（清缓存）** |

---

## 用起来之后的几个提醒

| 想做什么 | 怎么做 |
|---|---|
| 数据看着不对 / 怀疑是旧数据 | 右上角菜单 ⋮ → **刷新（清缓存）**（不会丢草稿，放心点） |
| 网页里返回上一页 | 手机返回键 |
| **后期迁到 NAS** | 右上角菜单 ⋮ → **设置服务器地址** → 填 NAS 地址（如 `http://192.168.31.x:8080`）<br>**不用重新打包 App** |
| 以后系统更新了 | 网页端更新后，App 里点「刷新（清缓存）」就是最新版，**不用重装** |

---

## 顺带一提

网页端我已经同步更新到 **v130** 并发布上线了。
你在**手机浏览器**上打开时记得**强刷一下**（下拉刷新，或清一下浏览器缓存），才能拿到新版。

App 装上之后就不用管网页缓存了——壳里已经把缓存关掉了。
